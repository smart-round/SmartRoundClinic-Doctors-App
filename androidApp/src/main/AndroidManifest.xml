<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name="ke.co.smartroundclinic.doctor.android.SmartRoundApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartRoundClinic">
        <activity
            android:exported="true"
            android:name="ke.co.smartroundclinic.doctor.android.MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|uiMode|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Keeps the call's audio/video running when the app is backgrounded during
             a consultation. We own this service (unlike the old UI Kit's KeepAliveService,
             which didn't even exist in the SDK version this app was pinned to). -->
        <service
            android:name="ke.co.smartroundclinic.doctor.presentation.main.chat.call.CallForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback|camera|microphone" />

        <!-- Full-screen ringing UI, auto-launched by the OS from the incoming-call full-screen-intent
             notification (see IncomingCallHandler) so it shows even over the lock screen. -->
        <activity
            android:name="ke.co.smartroundclinic.doctor.presentation.main.chat.call.IncomingCallActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:showOnLockScreen="true"
            android:theme="@style/Theme.SmartRoundClinic" />

        <!-- Handles the notification's Decline action button — the fallback path when the OS
             only shows a plain heads-up banner instead of launching IncomingCallActivity. -->
        <receiver
            android:name="ke.co.smartroundclinic.doctor.presentation.main.chat.call.IncomingCallActionReceiver"
            android:exported="false" />

        <!-- Doctor-to-doctor equivalents of the two above — separate components so a doctor call
             never collides with (or gets dismissed by) a concurrent patient call. -->
        <activity
            android:name="ke.co.smartroundclinic.doctor.presentation.main.chat.call.IncomingDoctorCallActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:showOnLockScreen="true"
            android:theme="@style/Theme.SmartRoundClinic" />

        <receiver
            android:name="ke.co.smartroundclinic.doctor.presentation.main.chat.call.IncomingDoctorCallActionReceiver"
            android:exported="false" />

        <!-- Handles the Picture-in-Picture window's own mute/end-call RemoteAction taps
             (see CallPictureInPicture.android.kt). -->
        <receiver
            android:name="ke.co.smartroundclinic.doctor.presentation.main.chat.call.CallPipActionReceiver"
            android:exported="false" />
    </application>

</manifest>
